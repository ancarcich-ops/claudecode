import ComingSoon from '@/components/ComingSoon';

export default function ProspectsPage() {
  return (
    <ComingSoon
      title="Top Prospects"
      blurb="A seeded Braves Top-30 you can re-rank from an admin screen, each linked to live stats and a player profile. Backed by a database so rankings persist and can be edited."
    />
  );
}
