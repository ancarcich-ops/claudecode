import ComingSoon from '@/components/ComingSoon';

export default function MoversPage() {
  return (
    <ComingSoon
      title="Risers & Slumpers"
      blurb="Daily stat snapshots feed a trend engine that flags who's hot or cold — rolling last-7 / last-15 performance vs. season, plus age-relative-to-level context. Requires the nightly ingestion job to accumulate history."
    />
  );
}
