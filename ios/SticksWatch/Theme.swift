//
//  Theme.swift
//  SticksWatch
//
//  Sticks design tokens tuned for the watch's black background.
//

import SwiftUI

extension Color {
    /// Sticks green lifted for legibility on black.
    static let sticksGreenBright = Color(red: 0.45, green: 0.76, blue: 0.56)

    /// Eagle / FINISH ROUND gold, matching the web palette — #A9762A
    static let sticksGold = Color(red: 169 / 255, green: 118 / 255, blue: 42 / 255)

    /// Deep accent green, matching the web palette — #285E45
    static let sticksGreen = Color(red: 40 / 255, green: 94 / 255, blue: 69 / 255)

    /// Over-par red, matching the web palette — #9A2B26
    static let sticksDanger = Color(red: 154 / 255, green: 43 / 255, blue: 38 / 255)

    /// Cream, matching the web palette — #EDE7DB
    static let sticksCream = Color(red: 237 / 255, green: 231 / 255, blue: 219 / 255)
}
